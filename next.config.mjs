/** @type {import('next').NextConfig} */
const nextConfig = {
    // Custom configuration options here
    images: {
      domains: ['images.pexels.com'], // Configure external domains for images
    },
    // Other Next.js configuration options can be added here
  };
  
  export default nextConfig;
  